"""部品ライブラリ。

プロジェクトの外に部品を置き、複数のプロジェクトから同じ部品を使い回す。
1 部品につき次の 2 つ（`.py` は必要な部品だけ）。

  <部品 id>.yaml   寸法・取付穴・コネクタ・出典・信頼度。BOM が `use:` で参照する
  <モジュール>.py  実形状モデル。YAML の `shape: "<module>:<関数>"` が指す

寸法はここだけが持つ。プロジェクトの `bom.yaml` に書けるのは「どう使うか」
（固定方法、クリアランス、CAD に配置するか、設計上の判断）だけで、寸法を
上書きしようとすると `load_bom()` がエラーにする。

実形状モデルの約束:
  - 部品を直方体で近似すると、回転軸やネジ穴の位置が実物とずれる
    （例: DYNAMIXEL XL330 の出力軸はケース中心ではなく上端寄り）
  - 原点は設計上意味のある基準（サーボなら「出力軸」）に置く
  - 出力軸・取付穴などの特徴点は関数で座標を返し、相手部品の設計に使う
  - 寸法の出所（datasheet/図面/STEP/実測）を docstring に明記する
"""
